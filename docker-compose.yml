version: '3.8'

services:
  api:
    build: ./backend
    volumes:
      - ./backend:/app
      - ./backend/uploads:/app/uploads
      - ./backend/outputs:/app/outputs
    environment:
      - COMFYUI_URL=http://localhost:1111
    network_mode: host
    restart: unless-stopped
